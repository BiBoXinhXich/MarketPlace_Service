const express = require("express");

const idValidation = require("../validations/id.validate");

const {} = require("../controllers/public.controller");

const router = express.Router();

// {host}/api/*

module.exports = router;